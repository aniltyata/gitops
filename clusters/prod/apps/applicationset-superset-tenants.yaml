apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: superset-tenants
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/aniltyata/gitops.git
        revision: main
        directories:
          - path: clusters/prod/tenants/*/*
  template:
    metadata:
      name: "superset-{{path[2]}}-{{path[3]}}"
      labels:
        app: superset
        tenant: "{{path[2]}}"
        instance: "{{path[3]}}"
    spec:
      project: default
      source:
        repoURL: https://github.com/YOUR_ORG/YOUR_GITOPS_REPO.git
        targetRevision: main
        path: "{{path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "tenant-{{path[2]}}-{{path[3]}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
